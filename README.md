# Dreamy Birthday Bottle

Minimal Vite + React project that presents a fullscreen magical bottle scene. Click the bottle to trigger particles, a tiny star burst, ambient sound, and a heartfelt quote.

Run locally:

```bash
npm install
npm run dev
```

This uses Tailwind CSS and Framer Motion. Audio starts on first interaction due to browser autoplay policies.
